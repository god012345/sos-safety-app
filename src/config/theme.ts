// 🎨 Color palette (dark, emergency-focused UI)
export const colors = {
  background: "#050816",      // App background
  card: "#0b1120",            // Cards / sections
  textPrimary: "#f9fafb",     // Main text
  textSecondary: "#9ca3af",   // Secondary text
  accent: "#ef4444",          // SOS / primary action (red)
  buttonBg: "#111827",        // Buttons
  border: "#1f2933",          // Borders / dividers
  success: "#10b981",         // Success / demo mode
  warning: "#f59e0b",         // Warnings
  danger: "#dc2626",          // Critical danger
  overlay: "rgba(0,0,0,0.85)",

};

// 📏 Spacing scale (consistent padding/margins)
export const spacing = {
  xs: 4,
  s: 8,
  m: 16,
  l: 24,
  xl: 32,
  xxl: 48,
};
